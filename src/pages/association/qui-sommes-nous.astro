---
import { getEntry } from 'astro:content';
import Layout from '../../layouts/Layout.astro';
import PageRenderer from '../../components/PageRenderer.astro';

const page = await getEntry('pages', 'qui-sommes-nous');
if (!page) throw new Error("content/pages/qui-sommes-nous.md introuvable");
---

<Layout title={page.data.title} description={page.data.description} ogSlug={page.id} noindex={page.data.noindex}>
  <PageRenderer page={page} />
</Layout>
