---
type Props = {
  inverse?: boolean;
};
const { inverse = false } = Astro.props;
---

<div class:list={['grid items-start gap-8 md:grid-cols-2', inverse && 'md:[&>*:first-child]:order-2']}>
  <div><slot name="gauche" /></div>
  <div><slot name="droite" /></div>
</div>
